# All Shop Market

It is app built to be able to buy and sell custom scripts, hosted by platform Vk Mini Apps.

## Instructions

* Clone the repo
* Run `npm install`
  > or `yarn`
* Run `npm start`
  > or `yarn start`
